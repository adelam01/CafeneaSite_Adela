@page
@model CafeneaSite.Pages.TipuriLapte.IndexModel

@{
    ViewData["Title"] = "Index";
}


<head>
    <link rel="stylesheet" href="~/css/IngredienteCSS.css" asp-append-version="true" />
</head>

<body>
    <h1>Index</h1>

    <p>
        <a asp-page="Create">Create New</a>
    </p>

    <div class="grid-container">
        @foreach (var item in Model.TipLapte)
        {
            <div class="grid-item">
                <h4>@item.DenumireLapte</h4>
                <img src="data:image/jpg;base64 , @item.Imagine" />
                <p>
                <div>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </div>
            </div>
        }
    </div>
</body>